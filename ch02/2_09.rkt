; 设区间 x 的下界为 x1，上界为 x2
; 两个区间的和的宽度 len1 = ((x2 + y2) - (x1 + y1)) / 2
; 两个区间的宽度的和 len2 = (x2 - x1) / 2 + (y2 - y1) / 2
; 知 len1 = len2
;
; 两个区间的差的宽度 len1 = ((x2 + y1) - (x1 + y2)) / 2
; 两个区间的宽度的和 len2 = (x2 - x1) / 2 - (y2 - y1) / 2
; 知 len1 = len2
;
; 举例 x = (1 3), y = (2 4)
; 两个区间的乘的宽度 len1 = (12 - 2) / 2 = 5
; 两个区间的宽度的乘 len2 = 1 x 1 = 1
; len1 != len2
;
; 除 同理
